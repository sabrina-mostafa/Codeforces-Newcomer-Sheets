#include<bits/stdc++.h>
using namespace std ;
int main ()
{
  double a, b, x, y, z;
  cin>>a>>b ;
  x=floor(a/b) ;
  y=ceil(a/b) ;
  z=round(a/b) ;
  printf("floor %.lf / %.lf = %.lf\nceil %.lf / %.lf = %.lf\nround %.lf / %.lf = %.lf",a,b,x,a,b,y,a,b,z);
}
